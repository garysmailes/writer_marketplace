<header class="authentication-header">
  <div class="authentication-mark" aria-hidden="true">
    WM
  </div>

  <h1 class="authentication-title"><%= title %></h1>

  <% if defined?(subtitle) && subtitle.present? %>
    <p class="authentication-subtitle"><%= subtitle %></p>
  <% end %>
</header>